file = { SOI ~ (line ~ NEWLINE+)* ~ EOI }

line = { a_instruction | line_other }
line_other = { (!NEWLINE ~ ANY)* }

a_instruction = { "@" ~ constant }
constant = @{ ASCII_DIGIT+ }

WHITESPACE = _{ " " | "\t" }
